{
  "name": "auction-queries",
  "version": "0.0.0",
  "author": "xandjiji <xandjiji@gmail.com>",
  "description": "Querying functions for Char Bazaar",
  "license": "Unlicense",
  "main": "dist",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rm -rf dist/ && tsc",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist"
  },
  "dependencies": {
    "shared-utils": "*"
  },
  "devDependencies": {
    "@types/exevo-pan": "*",
    "config": "*",
    "typescript": "4.3.5"
  }
}
